{
  "name": "y",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "rollupWatch": "rollup -c -w",
    "rollupOpts": "rollup",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.57.1"
  }
}
